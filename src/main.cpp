#include "server.hpp"
#include "FileOperation.hpp"

int main() {
	FileOperation	fs;

	// std::string	content = fs.readFile("example.txt");
	// fs.writeFile("/etc/asdf.txt", "aaaa");
	// std::cout << content;
	server();
	return 0;
}